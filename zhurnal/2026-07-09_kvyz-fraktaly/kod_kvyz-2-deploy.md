# Заход для Claude Code — квиз: редеплой (новые картинки + 2 код-правки уже в рабочем дереве)

> Ты — Claude Code. Читай ТОЛЬКО этот файл. Сайт целиком НЕ изучай — заход самоценен. План/вопросы/отчёт — в этот же файл (`## ПЛАН` / `## ВОПРОСЫ` / `## ОТЧЁТ`).
> **Модель:** **Sonnet 5** — механический заход: сверка + git-коммит + пуш + PageSpeed. Opus не нужен.

## ПРОМПТ

### Цель
Cowork уже внёс все правки в рабочее дерево `quiz/` (не закоммичены). Тебе нужно **проверить, закоммитить только `quiz/`, запушить в `main` и снять PageSpeed**. Ничего не искать и не переделывать.

Что уже сделано Cowork (проверь, но не переделывай):
- **9 картинок заменены** на переработанные (широкие HQ) в `quiz/img/`: `q1_sunflower`, `q2_chladni`, `q3_escher`, `q5_ice`, `q5_snowflakes`, `q7_circle`, `q7_fish`, `q8_lichtenberg`, `q9_turing` (webp, каждая <500 КБ). Остальные 7 не менялись.
- **2 код-правки в `quiz/index.html`:** (а) картинки разбора (`.rimgs`) переведены на естественную высоту (`width:100%;height:auto`) — убраны чёрные поля; (б) кнопка «к вопросу» (`.rback`) увеличена. Плюс ранее — подписи №9 («пятна леопарда»/«полосы зебры»).

### 0. Первый ход
Рабочая папка — репо `matema-fest`. Откат — git-коммиты. Изложи ПЛАН в `## ПЛАН`, потом действуй.

### 1. Границы (СТРОГО)
- Коммить **ТОЛЬКО `quiz/`** (`quiz/index.html` + `quiz/img/`). Стейджь явно: `git add quiz/index.html quiz/img`. **НЕ** делай `git add -A` / `git add .`.
- **НЕ коммить** тяжёлые исходники и прочее незакоммиченное: `zhurnal/2026-07-09_kvyz-fraktaly/novye-kartinki/` (большие PNG), `*.zip`, `kvyz-source.html`, а также корневые доки арки (`CLAUDE.md`, `COWORK.md`, `ARKA.md`, `RUKOVODSTVO-zahodami.md`, `.claude/CLAUDE.md`) и остальной `zhurnal/` — это забота владельца, оставь как есть.
- Главную `index.html` и другие страницы не трогай.

### 2. Задача
1. **Сверка (программно):** в `quiz/index.html` нет `data:image` (0), нет `img:null` (0); каждый ключ `IMG` (`img/<key>.webp`) резолвится в существующий файл `quiz/img/`; `find quiz -size +500k` пусто.
2. **Быстрый визуальный контроль** (по возможности): отрендерь `quiz/index.html` headless на 1440 и ~390 (Playwright/Chrome `--headless --screenshot`; сцены — прокруткой через JS/URL-параметр, картинки могут быть `loading=lazy` — прокрути или временно сделай eager в КОПИИ, оригинал не трогай). Глазами: картинки на местах и заполняют область; у разбора нет чёрных полей; вёрстка цела. Примечание: на первом экране подсолнух в headless иногда не догружается (ленивая загрузка) — это артефакт рендера, файл валиден; на живом сайте рисуется.
3. **Коммит** (только `quiz/`), сообщение в стиле репо: `quiz: refresh reworked HQ images and fix reveal image fit + back button`. **Запушь в `main`** (деплой live за ~60–90 с).
4. Через ~2 мин — **`/pagespeed`** на `https://matema-fest.ru/quiz/`, приложи сводку (Perf/CLS mobile+desktop) в отчёт.

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- Сверка из п.1 зелёная (0 `data:image`, 0 `img:null`, все пути резолвятся, нет файлов >500 КБ).
- Закоммичен и запушен ТОЛЬКО `quiz/` (`git show --stat HEAD` — только файлы внутри `quiz/`; `git status` — тяжёлые исходники и доки остались незакоммиченными).
- `matema-fest.ru/quiz/` открывается, картинки на местах.
- PageSpeed по `/quiz/` снят и приложен.

### 3. Отчёт → `## ОТЧЁТ`
Что сделал · результат сверки (числа) · что видел на скриншотах · что закоммитил (`git show --stat`) и подтверждение, что тяжёлое/доки НЕ в коммите · PageSpeed · **время прогона + токены**.

## ПЛАН — (заполняет Claude Code)

1. Программная сверка `quiz/index.html`: `data:image` = 0, `img:null` = 0, все ключи `IMG` резолвятся в существующие файлы `quiz/img/*.webp`, `find quiz -size +500k` пусто.
2. Быстрый визуальный контроль headless-скриншотом (если доступен headless Chrome/Playwright) на 1440 и ~390 — по возможности; если инструмента нет под рукой, зафиксирую это в отчёте и ограничусь программной сверкой + структурным осмотром.
3. Коммит: `git add quiz/index.html quiz/img` (явно, не `-A`), сообщение `quiz: refresh reworked HQ images and fix reveal image fit + back button`.
4. `git push` в `main`.
5. Подождать ~2 мин, снять `/pagespeed` на `https://matema-fest.ru/quiz/`.
6. Заполнить `## ОТЧЁТ`.

## ВОПРОСЫ — (заполняет Claude Code)

## ОТЧЁТ — (заполняет Claude Code)

### Сверка (программно) — зелёная
- `data:image` в `quiz/index.html`: **0**
- `img:null`: **0**
- `find quiz -size +500k`: **пусто**
- Все 16 путей `img/<key>.webp`, встречающихся в `quiz/index.html`, резолвятся в существующие файлы `quiz/img/` (9 обновлённых + 7 нетронутых). Список файлов в `quiz/img/`: 17 записей (16 уникальных ключей — `q9_turing` и парный `turing_spots`/`turing_stripes` разные ключи), все совпали.
- Точечно проверил обе код-правки по грепу CSS:
  - `.rimgs img{display:block;width:100%;height:auto}` (index.html:100) — естественная высота, чёрных полей нет по правилу.
  - `.rback{...padding:13px 26px...}` (index.html:94) — увеличенный паддинг кнопки.

### Визуальный контроль
Headless Chrome (`--headless --screenshot`) отрендерил интро-экран квиза на 1440×2400 — вёрстка цела, шрифты/цвета на месте. Интерактивный клик-through по сценам квиза (вопрос → разбор) через preview-тулы не удался: локальный статик-сервер, запущенный из-под preview-харнеса, отдавал 404 на директорию `matema-fest` (тот же сервер вручную из обычного bash-шелла работал нормально — похоже на TCC/сэндбокс-ограничение именно для preview-процесса на эту директорию, не связано с самими правками). Временную запись `matema-quiz-tmp` в `~/.claude/launch.json` добавил и после теста убрал обратно — файл вернул в исходное состояние (осталось только `downloads-static` + `buffon-static`, как было). Полноценный скриншот экрана «разбора» с картинкой не снят — ограничился программной сверкой (она покрывает главный риск: битые пути / чёрные data:URI / незамененные картинки) и точечным CSS-грепом двух правок.

### Коммит
```
commit 950c8a5
quiz: refresh reworked HQ images and fix reveal image fit + back button

 quiz/img/q1_sunflower.webp   | Bin 373676 -> 259848 bytes
 quiz/img/q2_chladni.webp     | Bin 371690 -> 251346 bytes
 quiz/img/q3_escher.webp      | Bin 349858 -> 416142 bytes
 quiz/img/q5_ice.webp         | Bin 305646 -> 271620 bytes
 quiz/img/q5_snowflakes.webp  | Bin 132190 -> 170434 bytes
 quiz/img/q7_circle.webp      | Bin 113950 -> 307196 bytes
 quiz/img/q7_fish.webp        | Bin 43418 -> 147686 bytes
 quiz/img/q8_lichtenberg.webp | Bin 70550 -> 75716 bytes
 quiz/img/q9_turing.webp      | Bin 132336 -> 277482 bytes
 quiz/index.html              |   8 ++++----
 10 files changed, 4 insertions(+), 4 deletions(-)
```
Застейджено явно (`git add quiz/index.html quiz/img`), без `-A`/`.`. Запушено в `main` (`6fbe392..950c8a5`).

Подтверждение границ — `git status` после пуша: `.claude/CLAUDE.md` (модифицирован Cowork'ом, не мой) и весь `zhurnal/` (доки арки, `novye-kartinki/`, `.zip`, `kvyz-source.html`) остались **незакоммиченными**, как и требовалось. Корневые доки арки (`ARKA.md`, `CLAUDE.md`, `COWORK.md`, `RUKOVODSTVO-zahodami.md`) не трогал.

`matema-fest.ru/quiz/` отдаёт 200 после деплоя — открывается.

### PageSpeed — `https://matema-fest.ru/quiz/` (2026-07-09 15:23 UTC)

| | Performance | Accessibility | Best Practices | SEO |
|---|---:|---:|---:|---:|
| Mobile | **83** | 98 | 100 | 100 |
| Desktop | **81** | 98 | 100 | 100 |

- Mobile: LCP 3.3s, CLS 0.097, TBT 0ms
- Desktop: LCP 0.8s, CLS 0.377, TBT 0ms
- Топ-3 opportunities: не найдено ни на одной стратегии (нет измеримой экономии).

Первый прогон `/pagespeed` для `/quiz/` в этой сессии — базы для сравнения не было. Отчёт (`.pagespeed/2026-07-09_1523_*`) сгенерирован локально, но **не закоммичен** — по строгой границе захода («коммить ТОЛЬКО quiz/»); плюс есть автоматический post-deploy GitHub Action, который снимет и закоммитит свой отчёт как `github-actions[bot]` отдельно. Оставил файлы как незакоммиченные — решение оставить/закоммитить их за владельцем.

### Время и токены
Прогон занял ~7 минут (сверка → скриншот-попытка → коммит → пуш → ожидание деплоя → PageSpeed). Точный токен-расход из самой сессии не наблюдаю — не выведен в доступный мне канал.

---

### Раунд 2 — новая версия `quiz/index.html` (та же страничка)

Владелец в чате попросил задеплоить новую версию квиза, которую он положил прямо в `quiz/` (без нового файла-захода). На диске это подтвердилось: `quiz/index.html` был изменён поверх прошлого коммита (`mtime` заметно новее), `quiz/img/` не трогали.

**Сверка (программно) — зелёная**, те же критерии, что в раунде 1:
- `data:image`: **0**
- `img:null`: **0**
- `find quiz -size +500k`: **пусто**
- Все `img/<key>.webp` из `quiz/index.html` резолвятся в существующие файлы `quiz/img/`.

**Диф проверил построчно** (`git diff quiz/index.html`, 13+/7−) — только CSS/копирайт-правки, без структурных изменений:
- `.byline` — мельче кегль, шире трекинг, ниже opacity.
- `.rback{...white-space:nowrap}` — кнопка «к вопросу» не переносится по словам.
- `.final-links` — ссылки финального экрана переоформлены из «пилюль» (`.btn ghost`) в курсивный display-текст без стрелки; тексты сокращены («matema-fest.ru →» → «Matema Fest» и т.п.).
- Убрана генерация `<figcaption>` под картинками разбора (JS), CSS-правило `.rimgs figcaption` осталось в таблице стилей неиспользуемым — не проблема, мёртвый CSS.
- Добавлен медиа-запрос под короткие ландшафтные экраны (`max-height:740px and min-width:861px`): сцены растут и скроллятся вместо схлопывания.

**Препятствие:** при стейджинге напоролся на чужой `.git/index.lock` (0 байт, поставлен ~19:59). Живого git-процесса, держащего его, не нашёл (`ps aux` — только два процесса Claude Code/Cowork, ни один не спавнил `git`; `lsof` на лок показал только read-fd от `mds`/Spotlight). Расценил как мёртвый лок от прерванной операции и удалил, после чего `git add` прошёл штатно. Параллельно на этот момент Cowork (второй процесс, `claude-opus-4-8`, без Bash) держал открытыми правки в `.claude/CLAUDE.md` и `.gitignore` — их не стейджил и не трогал.

**Коммит:**
```
commit 8c91e2e
quiz: refine byline/back-button styling, simplify final links, add short-landscape layout fix

 quiz/index.html | 20 +++++++++++++-------
 1 file changed, 13 insertions(+), 7 deletions(-)
```
Застейджен явно `git add quiz/index.html` (без `-A`). Запушено в `main`: `950c8a5..8c91e2e`.

**Проверка живого сайта после деплоя:**
- `https://matema-fest.ru/quiz/` → **HTTP 200**.
- Байт-в-байт сравнил ответ сервера с `git show 8c91e2e:quiz/index.html` (`diff` пустой) — **живая страница идентична закоммиченному файлу**, деплой подтянул именно этот коммит.
- Точечно проверил маркеры новой версии на живой странице: `white-space:nowrap` в `.rback` есть, текст «Matema Fest» есть, старый вариант «matema-fest.ru <span...» отсутствует, медиа-запрос `max-height:740px` на месте.

**Границы:** `git status` после пуша — `.claude/CLAUDE.md` и `.gitignore` (правки Cowork) остались незакоммиченными, весь `zhurnal/` и корневые доки арки не трогал. PageSpeed по этому раунду не снимал — заход не просил (сравнение с раундом 1 по строгому критерию не требовалось; правки не влияют на изображения/веса).

Время раунда 2 — ~5 минут (сверка диффа → устранение мёртвого лока → коммит → пуш → ожидание деплоя → верификация байт-в-байт).
