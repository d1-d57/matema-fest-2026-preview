# Заход для Claude Code — квиз: финальный редеплой (подзаголовок + картинка №8)

> Ты — Claude Code. Читай ТОЛЬКО этот файл. Сайт целиком НЕ изучай — заход самоценен. План/вопросы/отчёт — в этот же файл (`## ПЛАН` / `## ВОПРОСЫ` / `## ОТЧЁТ`).
> **Модель:** **Sonnet 5** — механический заход: сверка + git-коммит только `quiz/` + пуш + PageSpeed. Opus не нужен.

## ПРОМПТ

### Цель
Cowork внёс две правки в рабочее дерево `quiz/` (не закоммичены). Нужно **проверить, закоммитить только `quiz/`, запушить в `main`, снять PageSpeed**. Ничего не искать и не переделывать.

Что уже сделано Cowork (проверь, не переделывай):
- **Подзаголовок героя** в `quiz/index.html` изменён (убран спойлер): теперь «Как одна и та же геометрия проступает в живом и неживом — от растений до кристаллов.» (было — с перечислением снежинки/кожи рыбы/следа молнии, что выдавало ответы).
- **Картинка №8** заменена: `quiz/img/q8_lichtenberg.webp` — теперь настоящая фигура Лихтенберга в прозрачном акриловом блоке (было — плазма-шар, научно неверно). 83 КБ.

### 0. Первый ход
Рабочая папка — репо `matema-fest`. Откат — git-коммиты. Изложи ПЛАН в `## ПЛАН`, потом действуй.

### 1. Границы (СТРОГО)
- Коммить **ТОЛЬКО `quiz/`** — стейджь явно: `git add quiz/index.html quiz/img`. **НЕ** делай `git add -A` / `git add .`.
- **НЕ коммить** остальное незакоммиченное: `zhurnal/` (доки арки, `novye-kartinki/` с тяжёлыми PNG, `*.zip`, `kvyz-source.html`), корневые доки (`CLAUDE.md`, `COWORK.md`, `ARKA.md`, `RUKOVODSTVO-zahodami.md`, `.claude/CLAUDE.md`) — забота владельца, оставь как есть.
- Главную `index.html` и другие страницы не трогай.

### 2. Задача
1. **Сверка (программно):** в `quiz/index.html` — 0 `data:image`, 0 `img:null`; каждый ключ `IMG` (`img/<key>.webp`) резолвится в существующий файл; `find quiz -size +500k` пусто; подзаголовок содержит «в живом и неживом — от растений до кристаллов».
2. **Быстрый визуальный контроль** (по возможности): рендер `quiz/index.html` headless (Chrome `--headless --screenshot`) — хотя бы интро (проверить новый подзаголовок) и, если получится проскроллить, экран №8 (фигура Лихтенберга в блоке заполняет область). Примечание: в headless картинки `loading=lazy` иногда не догружаются при программном скролле (артефакт рендера) — файлы валидны, на живом сайте рисуются; если экран №8 вышел чёрным в скриншоте, это ленивая загрузка, не дефект.
3. **Коммит** только `quiz/`, сообщение: `quiz: neutral hero subtitle + real Lichtenberg figure for Q8`. **Запушь в `main`**.
4. Через ~2 мин — **`/pagespeed`** на `https://matema-fest.ru/quiz/`, сводку (Perf/CLS mobile+desktop) в отчёт.

**КРИТЕРИЙ ГОТОВНОСТИ (может провалиться):**
- Сверка п.1 зелёная.
- Закоммичен/запушен ТОЛЬКО `quiz/` (`git show --stat HEAD` — только файлы внутри `quiz/`; `git status` — доки и тяжёлое остались незакоммиченными).
- `matema-fest.ru/quiz/` открывается; на №8 — фигура Лихтенберга в акриловом блоке; подзаголовок обновлён.
- PageSpeed снят и приложен.

### 3. Отчёт → `## ОТЧЁТ`
Что сделал · сверка (числа) · что видел на скриншотах · `git show --stat HEAD` + подтверждение границ (доки/тяжёлое не в коммите) · PageSpeed · **время прогона + токены**.

## ПЛАН — (заполняет Claude Code)

1. `git status` — подтвердить, что незакоммичены именно `quiz/index.html` и `quiz/img/q8_lichtenberg.webp` (плюс посторонние доки/`.claude/CLAUDE.md`/`.gitignore`, которые не трогаю).
2. Программная сверка `quiz/index.html`: `data:image` = 0, `img:null` = 0, все ключи `IMG` резолвятся в файлы, `find quiz -size +500k` пусто, подзаголовок содержит нужный текст.
3. Headless-скриншот интро (и по возможности экрана №8) через `chrome --headless --screenshot`.
4. `git add quiz/index.html quiz/img` (точечно, без `-A`) → commit `quiz: neutral hero subtitle + real Lichtenberg figure for Q8` → push в `main`.
5. `git show --stat HEAD` + `git status` — подтвердить, что в коммите только `quiz/`, а доки/тяжёлое остались незакоммиченными.
6. Подождать ~2 мин, снять `/pagespeed` на `https://matema-fest.ru/quiz/`, вписать сводку.
7. Заполнить `## ОТЧЁТ`.

## ВОПРОСЫ — (заполняет Claude Code)

## ОТЧЁТ — (заполняет Claude Code)

### Что сделал
Проверил обе правки Cowork в `quiz/`, закоммитил только их, запушил в `main`, снял PageSpeed на живом URL. Ничего не переделывал.

### Сверка (п.1, программно) — зелёная
- `data:image` в `quiz/index.html`: **0**
- `img:null`: **0**
- Ключи карты `IMG` (16 шт.) — все резолвятся в существующие файлы `quiz/img/*.webp` (проверено скриптом, `missing: []`)
- `find quiz -size +500k`: **пусто**
- Подзаголовок содержит: «в живом и неживом — от растений до кристаллов.» ✅
- `quiz/img/q8_lichtenberg.webp`: 83 248 байт (≈83 КБ)

### Что видел на скриншотах / изображениях
- Headless-скриншот интро (`chrome --headless --screenshot`, 430×932): новый подзаголовок отрисован полностью, без спойлеров про снежинки/кожу рыбы/след молнии.
- Экран №8 через SPA-скролл в headless не гонял (квиз — не простой скролл, а JS-навигация через `scrollToScene()`/клики; заведение полноценного клик-флоу через preview-сервер избыточно для этой точечной сверки). Вместо этого открыл сам файл `quiz/img/q8_lichtenberg.webp` напрямую — на нём настоящая фигура Лихтенберга (ветвящийся дендритный разряд) в прозрачном акриловом блоке на тёмном фоне, кадр плотный, объект заполняет область. Плазма-шара нет, подмена подтверждена по содержимому файла.

### `git show --stat HEAD`
```
commit 2b108bb5f5963d921d49b1e6dde03f829a69c3e0
    quiz: neutral hero subtitle + real Lichtenberg figure for Q8

 quiz/img/q8_lichtenberg.webp | Bin 75716 -> 83248 bytes
 quiz/index.html              |   2 +-
 2 files changed, 1 insertion(+), 1 deletion(-)
```
Пуш: `8c91e2e..2b108bb main -> main` — прошёл.

**Подтверждение границ:** в коммите только 2 файла внутри `quiz/`. После коммита `git status` по-прежнему показывает незакоммиченными: `.claude/CLAUDE.md`, `.gitignore` (модифицированы, не трогал), `ARKA.md`, `CLAUDE.md`, `COWORK.md`, `RUKOVODSTVO-zahodami.md`, весь `zhurnal/` (включая `novye-kartinki/`, `paket-kartinki-pravki.zip`, `kvyz-source.html`), `.pagespeed/*` — как и требовалось, не тронуто.

Побочный след: во время разведки для скриншота №8 создавал/удалил лишний `.claude/launch.json` в репо (оказался не тем файлом — preview-тулинг читает домашний `~/.claude/launch.json`, не репозиторный) — удалён, в `git status` не значится.

Также обнаружил и снял зависший `.git/index.lock` (0 байт, ~час старый, активного git-процесса не было) перед стейджингом — иначе `git add` падал с `fatal: Unable to create index.lock`.

### PageSpeed — `https://matema-fest.ru/quiz/`

| | Mobile Perf | Desktop Perf | Mobile CLS | Desktop CLS | LCP mobile/desktop |
|---|---:|---:|---:|---:|---:|
| до (15:23) | 83 | 81 | 0.097 | 0.377 | 3.3s / 0.8s |
| после (19:52) | 82 | 81 | 0.116 | 0.377 | 3.3s / 0.8s |

TBT — 0 ms в обоих прогонах, оба раза. Accessibility 98 / Best Practices 100 / SEO 100 — без изменений. Разброс perf/CLS mobile (83→82, 0.097→0.116) укладывается в обычный шум PSI между прогонами, привязать к замене картинки/подзаголовка нет оснований (десктоп не изменился вовсе). Топ-3 opportunities — по-прежнему пусто в обоих прогонах.

**Вердикт: ship it** — метрики держатся, регрессии нет.

Файлы отчёта: `.pagespeed/2026-07-09_1952_{mobile,desktop}.json`, `.pagespeed/2026-07-09_1952_summary.md` (не закоммичены — не входят в границы `quiz/`).

### Время прогона + токены
Прогон (от первого чтения захода до записи отчёта, с учётом паузы ~2 мин на прогрев CDN перед PageSpeed): ≈10 минут. Точный расход токенов сессии из самого диалога не виден — оставляю владельцу свериться по `/usage` или логам сессии, если нужно точное число.
